import { DefaultTheme } from "styled-components";
import {
    colors,
    fontFamily,
    borderRadius,
    border,
    media,
    spacing,
    fontSize,
    fontWeights,
    shadows,
} from "./declarations";

const theme: DefaultTheme = {
    colors,
    media,
    fontFamily,
    borderRadius,
    fontSize,
    spacing,
    border,
    fontWeights,
    shadows,
};

export default theme;
